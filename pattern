n=int(input("enter number of rows"))
for i in range (1,n+1):
	for j in range(1,i+1):
		print("*" ,end="") #end stops it from going to the next line by default 
	print("") #to go to the next line after completion of each loop
		
